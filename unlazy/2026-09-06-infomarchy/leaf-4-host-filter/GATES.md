G1: Heatmap cells merged across hosts  |  CHECK: cd /home/j_kro/.config/omarchy/plugins/nixfred.infomarchy && timeout 120 bun fleet.ts 2>/dev/null | python3 -c "import json,sys; d=json.loads(''.join(f['data'] for l in sys.stdin if l.strip() for f in [json.loads(l)] if f.get('type')=='chunk')); print(len(d.get('ai',{}).get('heatmap',{}).get('cells',[])))"  |  EXPECT: /168/
G2: Counts merged across hosts  |  CHECK: cd /home/j_kro/.config/omarchy/plugins/nixfred.infomarchy && timeout 120 bun fleet.ts 2>/dev/null | python3 -c "import json,sys; d=json.loads(''.join(f['data'] for l in sys.stdin if l.strip() for f in [json.loads(l)] if f.get('type')=='chunk')); c=d.get('ai',{}).get('counts',{}); print('hermes' in c and 'opencode' in c)"  |  EXPECT: True
G3: Host filter wired into InfoView sessions  |  CHECK: grep -c 'desk.hostFilter' /home/j_kro/.config/omarchy/plugins/nixfred.infomarchy/InfoView.qml  |  EXPECT: /[2-9]/
G4: Host filter wired into InfoView recent  |  CHECK: grep -A2 'visibleRecentTasks' /home/j_kro/.config/omarchy/plugins/nixfred.infomarchy/InfoView.qml | grep -c 'hostFilter'  |  EXPECT: /[1-9]/
