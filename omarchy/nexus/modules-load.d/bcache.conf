# Load the bcache module at boot.
#
# nexus's 3.6TB storage pool is a bcache device (/dev/sda backing +
# /dev/sdb cache -> /dev/bcache0). Arch does not load this module
# automatically and ships no bcache-tools package, so without this file
# /dev/bcache0 never appears and every data mount fails.
bcache
